function solution(a, b) {
    const idxA = BigInt(a)
    const idxB = BigInt(b)
    // console.log(idxA, idxB) //특정 number는 Number이 안됨 === parseInt
    // console.log(idxA+idxB)
    return String(idxA+idxB)
    
}